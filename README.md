# Тестирование корректности данных в json файлах

установи зависимости

bash
pip install -r requirements.txt


# ToDo

1. Проверить, что ["stockCode"] == "KU", ошибка "Документ для другого склада"
2. Все ["containers"]["invoices"]["boxes"]["lines"]["qty"] > 0, ошибка "Не корректное количество товара"
3. Все ["containers"]["invoices"]["boxes"]["lines"]["ean"] есть среди ["info"]["ean"], ошибка "Нет справочника номенклатуры для строк документа"
4. Все ["DM"]["ean"] есть среди ["info"]["ean"], ошибка "Нет справочника номенклатуры для серийных номеров"
5. Все ["DM"]["ean"] есть среди ["containers"]["invoices"]["boxes"]["lines"]["ean"], ошибка "Лишние серийные номера в документе"
6. Если есть элементы ["Sets"], то ["Sets"]["eanSet"] есть среди ["info"]["ean"], ошибка "Нет справочника номенклатуры для наборов"
7. Если есть элементы ["Sets"], то ["Sets"]["setComposition"]["eansku"] есть среди ["info"]["ean"], ошибка "Нет справочника номенклатуры для состава наборов"

